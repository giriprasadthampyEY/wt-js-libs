<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

-   [Contracts][1]
    -   [Parameters][2]
    -   [\_getInstance][3]
        -   [Parameters][4]
    -   [getIndexInstance][5]
        -   [Parameters][6]
    -   [getHotelInstance][7]
        -   [Parameters][8]
    -   [decodeLogs][9]
        -   [Parameters][10]
    -   [createInstance][11]
        -   [Parameters][12]
-   [HotelDataIndex][13]
    -   [Properties][14]
-   [HotelDescriptionInterface][15]
    -   [Properties][16]
-   [LocationInterface][17]
    -   [Properties][18]
-   [AdditionalContact][19]
    -   [Properties][20]
-   [ContactInterface][21]
    -   [Properties][22]
-   [ContactsInterface][23]
    -   [Properties][24]
-   [AddressInterface][25]
    -   [Properties][26]
-   [DataModelOptionsType][27]
    -   [Properties][28]
-   [DataModel][29]
    -   [Parameters][30]
    -   [getWindingTreeIndex][31]
        -   [Parameters][32]
    -   [getTransactionsStatus][33]
        -   [Parameters][34]
    -   [createWallet][35]
        -   [Parameters][36]
    -   [createInstance][37]
        -   [Parameters][38]
-   [OnChainHotel][39]
    -   [Parameters][40]
    -   [initialize][41]
    -   [dataIndex][42]
    -   [setLocalData][43]
        -   [Parameters][44]
    -   [toPlainObject][45]
        -   [Parameters][46]
    -   [\_editInfoOnChain][47]
        -   [Parameters][48]
    -   [createOnChainData][49]
        -   [Parameters][50]
    -   [updateOnChainData][51]
        -   [Parameters][52]
    -   [transferOnChainOwnership][53]
        -   [Parameters][54]
    -   [removeOnChainData][55]
        -   [Parameters][56]
    -   [createInstance][57]
        -   [Parameters][58]
-   [WTIndex][59]
    -   [Parameters][60]
    -   [addHotel][61]
        -   [Parameters][62]
    -   [updateHotel][63]
        -   [Parameters][64]
    -   [removeHotel][65]
        -   [Parameters][66]
    -   [transferHotelOwnership][67]
        -   [Parameters][68]
    -   [getHotel][69]
        -   [Parameters][70]
    -   [getAllHotels][71]
    -   [createInstance][72]
        -   [Parameters][73]
-   [WTLibsError][74]
    -   [Parameters][75]
-   [SmartContractInstantiationError][76]
-   [WalletError][77]
-   [MalformedWalletError][78]
-   [WalletStateError][79]
-   [WalletPasswordError][80]
-   [WalletSigningError][81]
-   [TransactionMiningError][82]
-   [OutOfGasError][83]
-   [InsufficientFundsError][84]
-   [TransactionRevertedError][85]
-   [NoReceiptError][86]
-   [InaccessibleEthereumNodeError][87]
-   [InputDataError][88]
-   [OffChainDataError][89]
-   [OffChainDataConfigurationError][90]
-   [OffChainDataRuntimeError][91]
-   [StoragePointerError][92]
-   [RemotelyBackedDatasetError][93]
-   [RemoteDataAccessError][94]
-   [RemoteDataReadError][95]
-   [WtLibsOptionsType][96]
    -   [Properties][97]
-   [WTLibs][98]
    -   [Parameters][99]
    -   [getWTIndex][100]
        -   [Parameters][101]
    -   [getTransactionsStatus][102]
        -   [Parameters][103]
    -   [createWallet][104]
        -   [Parameters][105]
    -   [getOffChainDataClient][106]
        -   [Parameters][107]
    -   [createInstance][108]
        -   [Parameters][109]
    -   [errors][110]
-   [HotelOnChainDataInterface][111]
    -   [Properties][112]
-   [TransactionOptionsInterface][113]
    -   [Properties][114]
-   [TransactionCallbacksInterface][115]
    -   [Properties][116]
-   [PreparedTransactionMetadataInterface][117]
    -   [Properties][118]
-   [HotelInterface][119]
    -   [Properties][120]
-   [WTIndexInterface][121]
    -   [Properties][122]
-   [OffChainDataAdapterInterface][123]
    -   [Properties][124]
-   [DataModelInterface][125]
    -   [Properties][126]
-   [RawLogRecordInterface][127]
    -   [Properties][128]
-   [DecodedLogRecordInterface][129]
    -   [Properties][130]
-   [TransactionDataInterface][131]
    -   [Properties][132]
-   [TxInterface][133]
    -   [Properties][134]
-   [TxReceiptInterface][135]
    -   [Properties][136]
-   [AdaptedTxResultInterface][137]
    -   [Properties][138]
-   [AdaptedTxResultsInterface][139]
    -   [Properties][140]
-   [WalletInterface][141]
    -   [Properties][142]
-   [KeystoreV3Interface][143]
    -   [Properties][144]
-   [OffChainDataClientOptionsType][145]
    -   [Properties][146]
-   [OffChainDataClient][147]
    -   [setup][148]
        -   [Parameters][149]
    -   [\_reset][150]
    -   [getAdapter][151]
        -   [Parameters][152]
-   [RemotelyBackedDataset][153]
    -   [bindProperties][154]
        -   [Parameters][155]
    -   [isObsolete][156]
    -   [markObsolete][157]
    -   [isDeployed][158]
    -   [markDeployed][159]
    -   [\_genericGetter][160]
        -   [Parameters][161]
    -   [\_genericSetter][162]
        -   [Parameters][163]
    -   [updateRemoteData][164]
        -   [Parameters][165]
    -   [createInstance][166]
-   [FieldDefType][167]
    -   [Properties][168]
-   [StoragePointer][169]
    -   [Parameters][170]
    -   [reset][171]
    -   [\_genericGetter][172]
        -   [Parameters][173]
    -   [\_detectSchema][174]
        -   [Parameters][175]
    -   [\_getOffChainDataClient][176]
    -   [\_initFromStorage][177]
        -   [Parameters][178]
    -   [\_downloadFromStorage][179]
    -   [toPlainObject][180]
        -   [Parameters][181]
    -   [createInstance][182]
        -   [Parameters][183]
-   [Utils][184]
    -   [Parameters][185]
    -   [isZeroAddress][186]
        -   [Parameters][187]
    -   [applyGasCoefficient][188]
        -   [Parameters][189]
    -   [getCurrentWeb3Provider][190]
    -   [getCurrentBlockNumber][191]
    -   [determineCurrentAddressNonce][192]
        -   [Parameters][193]
    -   [getTransactionReceipt][194]
        -   [Parameters][195]
    -   [getTransaction][196]
        -   [Parameters][197]
    -   [createInstance][198]
        -   [Parameters][199]
-   [Wallet][200]
    -   [Parameters][201]
    -   [setWeb3][202]
        -   [Parameters][203]
    -   [isDestroyed][204]
    -   [getAddress][205]
    -   [unlock][206]
        -   [Parameters][207]
    -   [signAndSendTransaction][208]
        -   [Parameters][209]
    -   [lock][210]
    -   [destroy][211]
    -   [createInstance][212]
        -   [Parameters][213]

## Contracts

Wrapper class for work with Winding Tree's Ethereum
smart contracts.

### Parameters

-   `web3`  

### \_getInstance

Generic method for getting an instance of `web3.eth.Contract`

#### Parameters

-   `name` **[string][214]** of contract, used in errors
-   `abi` **[Object][215]** specification of contract
-   `address` **[string][214]** on which we should look for the contract


-   Throws **[SmartContractInstantiationError][216]** When address is invalid
-   Throws **[SmartContractInstantiationError][216]** When no code is deployed on given address

Returns **web3.eth.Contract** Resulting wrapper contract

### getIndexInstance

Returns a representation of <a href="https://github.com/windingtree/wt-contracts/blob/v0.2.0/contracts/WTIndex.sol">WTIndex.sol</a>.

#### Parameters

-   `address` **[string][214]** 

Returns **web3.eth.Contract** Instance of an Index

### getHotelInstance

Returns a representation of <a href="https://github.com/windingtree/wt-contracts/blob/v0.2.0/contracts/hotel/Hotel.sol">Hotel.sol</a>.

#### Parameters

-   `address` **[string][214]** 

Returns **web3.eth.Contract** Instance of a Hotel

### decodeLogs

Decodes ethereum transaction log values. Currently supports
events from Index and Hotel smart contracts.

#### Parameters

-   `logs` **[Array][217]&lt;[RawLogRecordInterface][218]>** in a raw format

Returns **[Array][217]&lt;[DecodedLogRecordInterface][219]>** Decoded logs

### createInstance

Returns an initialized instance

#### Parameters

-   `web3` **Web3** instance created by `new Web3(provider)`

Returns **[Contracts][220]** 

## HotelDataIndex

Index file for more data URIs. This is the
initial document that blockchain is pointing to.

### Properties

-   `descriptionUri` **[string][214]** 

## HotelDescriptionInterface

-   **See: [https://github.com/windingtree/wt-js-libs/issues/125][221]**

Description of additional descriptive hotel data.

### Properties

-   `location` **([Promise][222]&lt;[LocationInterface][223]?> | [LocationInterface][223]?)?** 
-   `name` **([Promise][222]&lt;[string][214]> | [string][214])** 
-   `description` **([Promise][222]&lt;[string][214]> | [string][214])** 
-   `roomTypes` **([Promise][222]&lt;{}?> | {}?)?** 
-   `contacts` **([Promise][222]&lt;[ContactsInterface][224]> | [ContactsInterface][224])** 
-   `address` **([Promise][222]&lt;[AddressInterface][225]> | [AddressInterface][225])** 
-   `timezone` **([Promise][222]&lt;[string][214]> | [string][214])** 
-   `currency` **([Promise][222]&lt;[string][214]> | [string][214])** 
-   `images` **([Promise][222]&lt;[Array][217]&lt;[string][214]>?> | [Array][217]&lt;[string][214]>?)?** 
-   `amenities` **([Promise][222]&lt;[Array][217]&lt;[string][214]>?> | [Array][217]&lt;[string][214]>?)?** 
-   `updatedAt` **([Promise][222]&lt;[string][214]> | [string][214])** 

## LocationInterface

Generic GPS location.

### Properties

-   `latitude` **[number][226]??** 
-   `longitude` **[number][226]??** 

## AdditionalContact

Generic additional contact.

### Properties

-   `title` **[string][214]** 
-   `value` **[string][214]** 

## ContactInterface

Generic contact.

### Properties

-   `email` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `phone` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `url` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `ethereum` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `additionalContacts` **([Promise][222]&lt;[Array][217]&lt;[AdditionalContact][227]>?> | [Array][217]&lt;[AdditionalContact][227]>?)?** 

## ContactsInterface

A map of hotel contacts.

### Properties

-   `general` **[ContactInterface][228]** 

## AddressInterface

Generic address interface.

### Properties

-   `line1` **([Promise][222]&lt;[string][214]> | [string][214])?** 
-   `line2` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `postalCode` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `city` **([Promise][222]&lt;[string][214]> | [string][214])?** 
-   `state` **([Promise][222]&lt;[string][214]?> | [string][214]?)?** 
-   `country` **([Promise][222]&lt;[string][214]> | [string][214])?** 

## DataModelOptionsType

DataModelOptionsType options. May look like this:

    {
      "provider": 'http://localhost:8545',// or another Web3 provider
      "gasCoefficient": 2 // Optional, defaults to 2
    }

Type: {provider: ([string][214] \| [Object][215])?, gasCoefficient: [number][226]?}

### Properties

-   `provider` **([string][214] \| [Object][215])?** 
-   `gasCoefficient` **[number][226]?** 

## DataModel

DataModel

### Parameters

-   `options` **[DataModelOptionsType][229]** 

### getWindingTreeIndex

Returns an Ethereum backed Winding Tree index.

#### Parameters

-   `address` **[string][214]** 

Returns **WTIndexDataProvider** 

### getTransactionsStatus

Finds out in what state are Ethereum transactions. All logs
are decoded along the way and some metrics such as min/max blockAge
are computed. If you pass all transactions related to a single
operation (such as updateHotel), you may benefit from the computed
metrics.

#### Parameters

-   `txHashes` **[Array][217]&lt;[string][214]>** 

Returns **[Promise][222]&lt;[AdaptedTxResultsInterface][230]>** 

### createWallet

Returns a wallet instance for given JSON keystore.

#### Parameters

-   `jsonWallet` **[KeystoreV3Interface][231]** 

Returns **[Wallet][232]** 

### createInstance

Creates a configured DataModel instance.

#### Parameters

-   `options` **[DataModelOptionsType][229]** 

Returns **[DataModel][233]** 

## OnChainHotel

Wrapper class for a hotel backed by a smart contract on
Ethereum that's holding `dataUri` pointer to its data.

It provides an accessor to such data in a form of
`StoragePointer` instance under `dataIndex` property.
Every schema-specific implementation details
are dealt with in StoragePointer.

### Parameters

-   `web3Utils` **[Utils][234]** 
-   `web3Contracts` **[Contracts][220]** 
-   `indexContract` **[Object][215]** 
-   `address` **[string][214]** 

### initialize

Initializes the underlying RemotelyBackedDataset that actually
communicates with the on-chain stored data. If address was provided
in the contsructor, the RemotelyBackedDataset is marked as deployed
and can be used instantly.

### dataIndex

Async getter for `StoragePointer` instance.
Since it has to eventually access the `dataUri`
field stored on-chain, it is lazy loaded.
Any data structure that is accessed by StoragePointer
instance has for now be known beforehand, thus the whole
data format of hotel data on `dataUri` is specified here.

Returns **[Promise][222]&lt;[StoragePointer][235]>** 

### setLocalData

Update manager and dataUri properties. dataUri can never be nulled. Manager
can never be nulled. Manager can be changed only for an un-deployed
contract (without address).

#### Parameters

-   `newData` **[HotelOnChainDataInterface][236]** 

Returns **[Promise][222]&lt;void>** 

### toPlainObject

Helper method that transforms the whole hotel into a sync simple
JavaScript object only with data properties.

By default, all off-chain data is resolved recursively. If you want to
limit off-chain data only to a certain subtree, use the resolvedFields
parameter that accepts an array of paths in dot notation (`father.son.child`).
Every last piece of every path will be resolved recursively as well. An empty
list means no fields will be resolved.

Properties that represent an actual separate document have a format of

    {
      'ref': 'schema://original-url',
      'contents': {
        'actual': 'data'
      }
    }

#### Parameters

-   `resolvedFields` **[Array][217]&lt;[string][214]>?** 
-   `List` **resolvedFields** of fields to be resolved from off chain data, in dot notation.
    If an empty array is provided, no resolving is done. If the argument is missing, all fields are resolved.


-   Throws **[StoragePointerError][237]** when an adapter encounters an error while accessing the data

Returns **[Promise][222]&lt;PlainHotelInterface>** 

### \_editInfoOnChain

Generates transaction data and metadata for updating dataUri on-chain.
Used internally as a remoteSetter for `dataUri` property.
Transaction is not signed nor sent here.

#### Parameters

-   `transactionOptions` **[TransactionOptionsInterface][238]** 
-   `options` **[TransactionOptionsInterface][238]** object, only `from` property is currently used, all others are ignored in this implementation

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** resulting transaction metadata

### createOnChainData

Generates transaction data and metadata for creating new hotel contract on-chain.
Transaction is not signed nor sent here.

#### Parameters

-   `transactionOptions` **[TransactionOptionsInterface][238]** 
-   `options` **[TransactionOptionsInterface][238]** object, only `from` property is currently used, all others are ignored in this implementation

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** Transaction data and metadata, including the freshly created hotel instance.

### updateOnChainData

Generates transaction data and metadata required for all hotel-related data modification
by calling `updateRemoteData` on a `RemotelyBackedDataset`.

#### Parameters

-   `transactionOptions` **[TransactionOptionsInterface][238]** 
-   `options` **[TransactionOptionsInterface][238]** object that is passed to all remote data setters


-   Throws **[SmartContractInstantiationError][216]** When the underlying contract is not yet deployed.
-   Throws **[SmartContractInstantiationError][216]** When dataUri is empty.

Returns **[Promise][222]&lt;[Array][217]&lt;[PreparedTransactionMetadataInterface][239]>>** List of transaction metadata

### transferOnChainOwnership

This is potentially devastating, so it's better to name
this operation explicitly instead of hiding it under updateOnChainData.

Generates transaction data and metadata required for a hotel ownership
transfer.

#### Parameters

-   `newManager` **[string][214]** 
-   `transactionOptions` **[TransactionOptionsInterface][238]** 
-   `Address` **[string][214]** of a new manager
-   `options` **[TransactionOptionsInterface][238]** object, only `from` property is currently used, all others are ignored in this implementation


-   Throws **[SmartContractInstantiationError][216]** When the underlying contract is not yet deployed.

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** Transaction data and metadata, including the freshly created hotel instance.

### removeOnChainData

Generates transaction data and metadata required for destroying the hotel object on network.

#### Parameters

-   `transactionOptions` **[TransactionOptionsInterface][238]** 
-   `options` **[TransactionOptionsInterface][238]** object, only `from` property is currently used, all others are ignored in this implementation


-   Throws **[SmartContractInstantiationError][216]** When the underlying contract is not yet deployed.

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** Transaction data and metadata, including the freshly created hotel instance.

### createInstance

Create new configured instance.

#### Parameters

-   `web3Utils` **[Utils][234]** 
-   `web3Contracts` **[Contracts][220]** 
-   `indexContract` **web3.eth.Contract** Representation of Winding Tree index
-   `address` **[string][214]** is an optional pointer to Ethereum network where the hotel lives.
    It is used as a reference for on-chain stored data. If it is not provided, a hotel has
    to be created on chain to behave as expected.

Returns **[OnChainHotel][240]** 

## WTIndex

Ethereum smart contract backed implementation of Winding Tree
index wrapper. It provides methods for working with hotel
contracts.

### Parameters

-   `indexAddress` **[string][214]** 
-   `web3Utils` **[Utils][234]** 
-   `web3Contracts` **[Contracts][220]** 

### addHotel

Generates transaction data required for adding a totally new hotel
and more metadata required for sucessful mining of that transaction.
Does not sign or send the transaction.

#### Parameters

-   `hotelData` **[HotelOnChainDataInterface][236]** 


-   Throws **[InputDataError][241]** When hotelData does not contain dataUri property.
-   Throws **[InputDataError][241]** When hotelData does not contain a manager property.
-   Throws **[WTLibsError][242]** When anything goes wrong during data preparation phase.

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 

### updateHotel

Generates a list of transaction data required for updating a hotel
and more metadata required for sucessful mining of those transactions.
Does not sign or send any of the transactions.

#### Parameters

-   `hotel` **[HotelInterface][243]** 


-   Throws **[InputDataError][241]** When hotel does not have a manager field.
-   Throws **[InputDataError][241]** When hotel does not contain a manager property.
-   Throws **[WTLibsError][242]** When anything goes wrong during data preparation phase.

Returns **[Promise][222]&lt;[Array][217]&lt;[PreparedTransactionMetadataInterface][239]>>** 

### removeHotel

Generates transaction data required for removing a hotel
and more metadata required for successful mining of that transaction.
Does not sign or send the transaction.

#### Parameters

-   `hotel` **[HotelInterface][243]** 


-   Throws **[InputDataError][241]** When hotel does not contain dataUri property.
-   Throws **[InputDataError][241]** When hotel does not contain a manager property.
-   Throws **[WTLibsError][242]** When anything goes wrong during data preparation phase.

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 

### transferHotelOwnership

Generates transaction data required for transferring a hotel
ownership and more metadata required for successful mining of that
transactoin. Does not sign or send the transaction.

#### Parameters

-   `hotel` **[HotelInterface][243]** 
-   `newManager` **[string][214]** 


-   Throws **[InputDataError][241]** When hotel does not have an address.
-   Throws **[InputDataError][241]** When hotel does not contain a manager property.
-   Throws **[InputDataError][241]** When the new manager address is the same as the old manager.
-   Throws **[InputDataError][241]** When the new manager address is not a valid address.
-   Throws **[WTLibsError][242]** When anything goes wrong during data preparation phase.

Returns **[Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 

### getHotel

Gets hotel representation of a hotel on a given address. If hotel
on such address is not registered through this Winding Tree index
instance, the method throws immediately.

#### Parameters

-   `address` **[string][214]** 


-   Throws **[WTLibsError][242]** When hotel does not exist.
-   Throws **[WTLibsError][242]** When something breaks in the network communication.

Returns **[Promise][222]&lt;[HotelInterface][243]?>** 

### getAllHotels

Returns a list of all hotels. It will filter out
every hotel that is inaccessible for any reason.

Currently any inaccessible hotel is silently ignored.
Subject to change.

Returns **[Promise][222]&lt;[Array][217]&lt;[HotelInterface][243]>>** 

### createInstance

Returns a configured instance of WTIndex
representing a Winding Tree index contract on a given `indexAddress`.

#### Parameters

-   `indexAddress` **[string][214]** 
-   `web3Utils` **[Utils][234]** 
-   `web3Contracts` **[Contracts][220]** 

Returns **[WTIndex][244]** 

## WTLibsError

**Extends Error**

Generic WT Libs error.

### Parameters

-   `message`  
-   `originalError`  

## SmartContractInstantiationError

**Extends WTLibsError**

A smart contract abstraction could not have been created.

## WalletError

**Extends WTLibsError**

Some error occurred in the wallet abstraction.

## MalformedWalletError

**Extends WalletError**

Thrown when a wallet cannot be decrypted by web3 due to its format
or bad parameters.

## WalletStateError

**Extends WalletError**

Thrown when an operation is performed with a wallet
that can not be currently done due to its internal state.

## WalletPasswordError

**Extends WalletError**

Wallet cannot be decrypted because of a bad password.

## WalletSigningError

**Extends WalletError**

Error occured during signing a transaction, that might
mean that the wallet can't sign the transaction data,
because tx.from does not match the wallet's address.

## TransactionMiningError

**Extends WTLibsError**

Generic error that happens when no special case is
detected during transaction mining error throw by web3.js

## OutOfGasError

**Extends TransactionMiningError**

Transaction supposedly ran out of gas and was not mined.
This depends on how precisely the error is reported by EVM
and subsequently web3.js

## InsufficientFundsError

**Extends TransactionMiningError**

Transaction supposedly was not mined because the originating
account does not have enough funds.
This depends on how precisely the error is reported by EVM
and subsequently web3.js

## TransactionRevertedError

**Extends TransactionMiningError**

Transaction was supposedly reverted and was not mined.
This depends on how precisely the error is reported by EVM
and subsequently web3.js

## NoReceiptError

**Extends TransactionMiningError**

There was a problem with getting a transaction receipt (network issue,
timeout, small gasPrice). It does not necessarily mean that the
transaction was not mined. You should probably check for it by other means.
This depends on how precisely the error is reported by EVM
and subsequently web3.js

## InaccessibleEthereumNodeError

**Extends TransactionMiningError**

There was a problem with communicating with the Ethereum node.
The library has literally zero idea about what really happened with the
transaction.

## InputDataError

**Extends WTLibsError**

Input data has a wrong format or some data is missing.

## OffChainDataError

**Extends WTLibsError**

Generic error related to off-chain stored data.

## OffChainDataConfigurationError

**Extends OffChainDataError**

An error occurred during a configuration of off-chain data storages.

## OffChainDataRuntimeError

**Extends OffChainDataError**

An error occurred during an attempt to use off-chain data storage.

## StoragePointerError

**Extends WTLibsError**

An error occurred when working with a StoragePointer.

## RemotelyBackedDatasetError

**Extends WTLibsError**

Generic error that occurrs during any work with RemotelyBackedDataset.

## RemoteDataAccessError

**Extends RemotelyBackedDatasetError**

It is impossible to access remote data (i. e. the data is in bad state).

## RemoteDataReadError

**Extends RemotelyBackedDatasetError**

It is impossible to read from remote data (i. e. the connection might have been lost).

## WtLibsOptionsType

General options for wt-libs-js. Holds all things necessary
for successful setup of Winding Tree network.

Type: {dataModelOptions: [DataModelOptionsType][229], offChainDataOptions: [OffChainDataClientOptionsType][245]}

### Properties

-   `dataModelOptions` **[DataModelOptionsType][229]** 
-   `offChainDataOptions` **[OffChainDataClientOptionsType][245]** 

## WTLibs

Main public interface of wt-libs-js.

### Parameters

-   `options` **[WtLibsOptionsType][246]** 

### getWTIndex

Get an instance of Winding Tree index from the underlying `data-model`.

#### Parameters

-   `address` **[string][214]** of the Winding Tree index

Returns **[WTIndexInterface][247]** 

### getTransactionsStatus

Get a transactions status from the underlying `data-model`.
This method is async because it communicates directly with and EVM node.

#### Parameters

-   `transactionHashes` **[Array][217]&lt;[string][214]>** 

Returns **[Promise][222]&lt;[AdaptedTxResultsInterface][230]>** 

### createWallet

Returns a wallet abstraction that can be used for signing transactions

#### Parameters

-   `jsonWallet` **[KeystoreV3Interface][231]** 

Returns **[WalletInterface][248]** 

### getOffChainDataClient

Returns an off-chain data storage client that can be used for uploading
or downloading data stored off-chain.

#### Parameters

-   `schema` **[string][214]** 

Returns **[OffChainDataAdapterInterface][249]** 

### createInstance

Call this to create wt-libs-js instance.

#### Parameters

-   `options` **[WtLibsOptionsType][246]** 

Returns **[WTLibs][250]** WTLibs

### errors

A map of errors that WTLibs can throw, useful
for checking what happened in your code.

## HotelOnChainDataInterface

Shape of data that is stored on-chain
about every hotel.

-   `address` is the network address.
-   `manager` is the network address of hotel manager.
-   `dataUri` holds a pointer to the off-chain storage
    that is used internally to store data.

### Properties

-   `address` **([Promise][222]&lt;[string][214]?> | [string][214]?)** 
-   `manager` **([Promise][222]&lt;[string][214]?> | [string][214]?)** 
-   `dataUri` **([Promise][222]&lt;[string][214]?> | [string][214]?)** 

## TransactionOptionsInterface

Ethereum transaction options that are passed from an external user.
It has to contain `from` and usually would contain `to` as well.

This copies the structure of [https://web3js.readthedocs.io/en/1.0/web3-eth-contract.html#contract-estimategas][251]
as it might be used as a base for gas estimation prior to actually
sending a transaction.

### Properties

-   `from` **[string][214]** 
-   `to` **[string][214]?** 
-   `gas` **[number][226]?** 
-   `value` **([number][226] \| [string][214] | BigNumber)?** 

## TransactionCallbacksInterface

Callback options that can be passed to a transaction that
will be signed and sent through our Wallet abstraction.

### Properties

-   `onReceipt` **function (receipt: [TxReceiptInterface][252]): void?** 
-   `onTransactionHash` **function (hash: [string][214]): void?** 

## PreparedTransactionMetadataInterface

Format of generated transaction data and metadata
that contains a related hotel instance, transactionData
itself (ready for signing) and optionally eventCallbacks
that should be passed to our Wallet abstraction with
transactionData itself to ensure a consistent internal state
after the transaction is mined.

### Properties

-   `hotel` **[HotelInterface][243]** 
-   `transactionData` **[TransactionDataInterface][253]** 
-   `eventCallbacks` **[TransactionCallbacksInterface][254]?** 

## HotelInterface

**Extends HotelOnChainDataInterface**

Represents a hotel instance that can
communicate with on-chain hotel representation
and provides an access to offChain data via `dataIndex`
property.

### Properties

-   `dataIndex` **[Promise][222]&lt;[StoragePointer][235]>** 
-   `toPlainObject` **function (): [Promise][222]&lt;[Object][215]>** 
-   `setLocalData` **function (newData: [HotelOnChainDataInterface][236]): [Promise][222]&lt;void>** 
-   `createOnChainData` **function (transactionOptions: [TransactionOptionsInterface][238]): [Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 
-   `updateOnChainData` **function (transactionOptions: [TransactionOptionsInterface][238]): [Promise][222]&lt;[Array][217]&lt;[PreparedTransactionMetadataInterface][239]>>** 
-   `removeOnChainData` **function (transactionOptions: [TransactionOptionsInterface][238]): [Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 
-   `transferOnChainOwnership` **function (newManager: [string][214], transactionOptions: [TransactionOptionsInterface][238]): [Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 

## WTIndexInterface

WindingTree index interface that provides all methods
necessary for interaction with the hotels.\`

### Properties

-   `addHotel` **function (hotel: [HotelOnChainDataInterface][236]): [Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 
-   `getHotel` **function (address: [string][214]): [Promise][222]&lt;[HotelInterface][243]?>** 
-   `getAllHotels` **function (): [Promise][222]&lt;[Array][217]&lt;[HotelInterface][243]>>** 
-   `updateHotel` **function (hotel: [HotelInterface][243]): [Promise][222]&lt;[Array][217]&lt;[PreparedTransactionMetadataInterface][239]>>** 
-   `removeHotel` **function (hotel: [HotelInterface][243]): [Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 
-   `transferHotelOwnership` **function (hotel: [HotelInterface][243], newManager: [string][214]): [Promise][222]&lt;[PreparedTransactionMetadataInterface][239]>** 

## OffChainDataAdapterInterface

Interface for an off-chain storage read.

### Properties

-   `upload` **function (data: {}): [Promise][222]&lt;[string][214]>** 
-   `update` **function (uri: [string][214], data: {}): [Promise][222]&lt;[string][214]>** 
-   `download` **function (uri: [string][214]): [Promise][222]&lt;{}?>** 

## DataModelInterface

Formalization of DataModel's public interface.

### Properties

-   `getWindingTreeIndex` **function (address: [string][214]): [WTIndexInterface][247]** 
-   `getTransactionsStatus` **function (transactionHashes: [Array][217]&lt;[string][214]>): [Promise][222]&lt;[AdaptedTxResultsInterface][230]>** 
-   `createWallet` **function (jsonWallet: [Object][215]): [WalletInterface][248]** 

## RawLogRecordInterface

This interface represents raw ethereum transaction log object
as returned by <a href="http://web3js.readthedocs.io/en/1.0/web3-eth.html#eth-getpastlogs-return">getPastLogs</a>.
Sometimes you might need the raw data to do some additional processing.

### Properties

-   `address` **[string][214]** 
-   `data` **[string][214]** 
-   `topics` **[Array][217]&lt;[string][214]>** 
-   `logIndex` **[number][226]** 
-   `transactionIndex` **[number][226]** 
-   `transactionHash` **[number][226]** 
-   `blockHash` **[number][226]** 
-   `blockNumber` **[number][226]** 

## DecodedLogRecordInterface

This interface represents an Ethereum log record
with decoded data that are much easier to read
and act upon.

### Properties

-   `address` **[string][214]** 
-   `event` **[string][214]** 
-   `attributes` **[Array][217]&lt;{name: [string][214], type: [string][214], value: [string][214]}>** 

## TransactionDataInterface

Ethereum transaction data used when creating transaction, see for example
[https://web3js.readthedocs.io/en/1.0/web3-eth-accounts.html#signtransaction][255]

### Properties

-   `nonce` **[number][226]??** 
-   `chainId` **[string][214]?** 
-   `from` **[string][214]** 
-   `to` **[string][214]** 
-   `data` **[string][214]** 
-   `value` **[string][214]?** 
-   `gasPrice` **[string][214]?** 
-   `gas` **([string][214] \| [number][226])** 

## TxInterface

Ethereum transaction data after TX was accepted by the network, see
for example [http://web3js.readthedocs.io/en/1.0/web3-eth.html#gettransaction][256]

### Properties

-   `hash` **[string][214]?** 
-   `nonce` **([string][214] \| [number][226])?** 
-   `blockHash` **[string][214]?** 
-   `blockNumber` **[number][226]?** 
-   `transactionIndex` **[number][226]?** 
-   `from` **[string][214]?** 
-   `to` **[string][214]?** 
-   `value` **[string][214]?** 
-   `gasPrice` **[string][214]?** 
-   `gas` **[number][226]?** 
-   `input` **[string][214]?** 

## TxReceiptInterface

Transaction receipt as returned by
<a href="http://web3js.readthedocs.io/en/1.0/web3-eth.html#eth-gettransactionreceipt-return">getTransactionReceipt</a>.
This raw data might be sometimes needed for additional processing.

### Properties

-   `transactionHash` **[string][214]** 
-   `blockNumber` **[number][226]** 
-   `blockHash` **[string][214]** 
-   `transactionIndex` **[number][226]** 
-   `from` **[string][214]** 
-   `to` **[string][214]** 
-   `contractAddress` **any** 
-   `cumulativeGasUsed` **[number][226]** 
-   `gasUsed` **[number][226]** 
-   `logs` **[Array][217]&lt;[RawLogRecordInterface][218]>** 
-   `status` **[boolean][257]** 

## AdaptedTxResultInterface

A custom transaction result interface that informs
about the transaction status, its age and decoded logs.

### Properties

-   `transactionHash` **[string][214]** 
-   `blockAge` **[number][226]** 
-   `decodedLogs` **[Array][217]&lt;[DecodedLogRecordInterface][219]>** 
-   `raw` **[TxReceiptInterface][252]** 

## AdaptedTxResultsInterface

A cummulative result of multiple transactions. We are
computing how many of the transactions were already
executed, how old are they (which might be useful for making
assumptions about confirmations). This also contains the raw data.

### Properties

-   `meta` **{total: [number][226], processed: [number][226], minBlockAge: [number][226], maxBlockAge: [number][226], allPassed: [boolean][257]}** 
-   `meta.total` **[number][226]** 
-   `meta.processed` **[number][226]** 
-   `meta.minBlockAge` **[number][226]** 
-   `meta.maxBlockAge` **[number][226]** 
-   `meta.allPassed` **[boolean][257]** 
-   `results` **{}?** 

## WalletInterface

Wallet abstraction interface. It assumes the following workflow:
1\. libs user holds a json wallet
2\. libs user unlocks the wallet abstraction with a password
3\. libs user calls some business logic which internally uses `signAndSendTransaction`
4\. libs user either locks the wallet (if she plans to use it again)
4\. OR destroys the wallet object data by calling `destroy`

`lock` should not remove the data necessary for unlocking the wallet again.
`destroy` on the other hand should clean all data that may be exploited from memory

### Properties

-   `unlock` **function (password: [string][214]): void** 
-   `signAndSendTransaction` **function (transactionData: [TransactionDataInterface][253], eventCallBacks: [TransactionCallbacksInterface][254]): [Promise][222]&lt;([string][214] \| [TxReceiptInterface][252])>** 
-   `lock` **function (): void** 
-   `destroy` **function (): void** 
-   `getAddress` **function (): [string][214]** 

## KeystoreV3Interface

Interface for Ethereum keystore

Description: [https://medium.com/@julien.m./what-is-an-ethereum-keystore-file-86c8c5917b97][258]

Specification: [https://github.com/ethereum/wiki/wiki/Web3-Secret-Storage-Definition][259]

### Properties

-   `version` **[number][226]** 
-   `id` **[string][214]** 
-   `address` **[string][214]** 
-   `crypto` **{ciphertext: [string][214], cipherparams: {iv: [string][214]}, cipher: [string][214], kdf: [string][214], kdfparams: {dklen: [number][226], salt: [string][214], n: [number][226], r: [number][226], p: [number][226]}, mac: [string][214]}** 
-   `crypto.ciphertext` **[string][214]** 
-   `crypto.cipherparams` **{iv: [string][214]}** 
-   `crypto.cipherparams.iv` **[string][214]** 
-   `crypto.cipher` **[string][214]** 
-   `crypto.kdf` **[string][214]** 
-   `crypto.kdfparams` **{dklen: [number][226], salt: [string][214], n: [number][226], r: [number][226], p: [number][226]}** 
-   `crypto.kdfparams.dklen` **[number][226]** 
-   `crypto.kdfparams.salt` **[string][214]** 
-   `crypto.kdfparams.n` **[number][226]** 
-   `crypto.kdfparams.r` **[number][226]** 
-   `crypto.kdfparams.p` **[number][226]** 
-   `crypto.mac` **[string][214]** 

## OffChainDataClientOptionsType

OffChainDataClientOptionsType

Type: {adapters: {}}

### Properties

-   `adapters` **{}** 

## OffChainDataClient

OffChainDataClient is a static factory class that is responsible
for creating proper instances of OffChainDataAdapterInterface.
It is configured during the library initialization.

Please bear in mind, that once the adapters are configured, the
configuration is shared during the whole runtime.

### setup

Initializes the map of OffChainDataAdapters.

#### Parameters

-   `options` **[OffChainDataClientOptionsType][245]** 
-   `OffChainDataClientOptionsType`  


-   Throws **[OffChainDataConfigurationError][260]** when there are multiple adapters with the same name

### \_reset

Drops all pre-configured OffChainDataAdapters. Useful for testing.

### getAdapter

Returns a fresh instance of an appropriate OffChainDataAdapter by
calling the `create` function from the adapter's configuration.

#### Parameters

-   `schema` **[string][214]?** 


-   Throws **[OffChainDataRuntimeError][261]** when schema is not defined or adapter for this schema does not exist

Returns **[OffChainDataAdapterInterface][249]** 

## RemotelyBackedDataset

Dataset ready to use various strategies for storing the data
in a remote storage. Every field backed by this strategy should
have a getter and setter defined that interacts with the
remote storage. The dataset strategy may be in the following states:

-   fresh - Purely in-memory created object with no data
-   unsynced - Some data may be set locally, but they were not
    propagated to the remote storage, and no data was loaded from the
    remote storage.
-   deployed - Data has its representation ready on the remote storage
    and we may call remote getters and setters to interact with the remote storage.
-   obsolete - Data lost its representation on the remote storage and
    we should not interact with it anymore.

Internally a state is held for each property. If you `get` a property
that was not previously accessed, the whole dataset gets synced (this might
get more efficient in the future), and you get a current value from
the remote storage. If any value was changed locally, it is considered
as the current value. Once you are done with data modification, you
have to call `updateRemoteData` that propagates the whole dataset to the
remote storage. These calls are deduplicated, so if a single call is used
to update multiple properties, only once call is done.

### bindProperties

Creates generic getters and setters that proxy `remoteGetter` and
`remoteSetter` when necessary.

The fields are specified as an `options.fields` property and every key
represents a single property. Every property's options can than hold
a `remoteGetter` and `remoteSetter` field such as

    {fields: {
        dataUri: {
          remoteGetter: async (): Promise<?string> => {
            return (await this.contract.dataUri().call();
          },
          // this will usually return a transaction ID
          remoteSetter: async (): Promise<string> => {
            return this.contract.methods.callHotel(this.address, data).send(txOptions);
          }
    },

All passed fields are set as unsynced which means that
after the first `get` on any of those, the whole dataset will
be synced from the remote storage (if the dataset is marked as deployed).

#### Parameters

-   `options` **[Object][215]** `{fields: {[field]: fieldOptions}}`
-   `bindTo` **[Object][215]** Object to which the properties will be bound.
    Typically the initiator of this operation.

### isObsolete

Is dataset marked as obsolete?

Returns **[Boolean][257]** 

### markObsolete

Marks dataset as obsolete. Typically called after the remote storage
is destroyed or made inaccessible. This is not propagated anywhere
but merely serves as a flag to prevent further interaction with this object.

### isDeployed

Is dataset deployed to the remote storage?

Returns **[Boolean][257]** 

### markDeployed

Marks dataset as deployed. Typically called when the remote
storage is set up, created or connected to.

### \_genericGetter

Tries to get a value. If the property was not synced before,
it will sync the whole dataset from a remote storage. If a property
was modified locally before, the modified value will be returned.

#### Parameters

-   `property` **[string][214]** 


-   Throws **[RemoteDataAccessError][262]** When dataset is marked as obsolete

Returns **any** property's current value

### \_genericSetter

Sets a new value locally and marks the property as dirty. Thath
means that even after syncing data from remote storage, the object will still
serve the locally modified value.

#### Parameters

-   `property` **[string][214]** 
-   `newValue` **any** 

### updateRemoteData

Calls all remoteSetters if relevant data was changed.
Calls are deduplicated, so if the same method would be used
to update multiple fields, it is called only once.

#### Parameters

-   `transactionOptions` **[Object][215]** passed to every remoteSetter, typically something like `{from: address, to: address}`

Returns **[Array][217]&lt;any>** Results of remoteSetters, it would typically contain transaction metadata. In any case, an eventCallbacks
object is appended to every result and onReceipt callback is added to ensure that data fields would eventually be properly marked as 'synced'.

### createInstance

Generic factory method.

## FieldDefType

Definition of a data field that is stored off-chain.
This may be recursive.

Type: {name: [string][214], isStoragePointer: [boolean][257]?, fields: [Array][217]&lt;([FieldDefType][263] \| [string][214])>?}

### Properties

-   `name` **[string][214]** 
-   `isStoragePointer` **[boolean][257]?** 
-   `fields` **[Array][217]&lt;([FieldDefType][263] \| [string][214])>?** 

## StoragePointer

`StoragePointer` serves as a representation of an
off-chain document holding JSON data. This generic class
does not enforce any protocol/schema and contains
infrastructure code that helps to set up field definition
and getters for every data field. It does not provide any
means of writing data, its sole purpose is for reading.

It is possible to use this recursively, so you can define
a field as another storage pointer. The configuration is
declarative and may look like this:

    const pointer = StoragePointer.createInstance('some://url', ['name', 'description']);
    pointer.ref; // contains 'some://url',
    await pointer.contents.name;
    await pointer.contents.description;

Or in recursive cases:

    const pointer = StoragePointer.createInstance('some://url', [
      {
        name: 'description',
        isStoragePointer: true,
        fields: ['name', 'description', 'location'],
      },
      'signature'
    ]);
    pointer.ref; // contains  'some://url'
    await pointer.contents.signature;
    const descPointer = await pointer.contents.description;
    descPointer.ref; // contains whatever is written in a description property in a document located on 'some://url'
    await descPointer.contents.name;

Only a top-level properties have to be defined beforehand, so the `signature`
field above may contain a complex JSON object.

### Parameters

-   `uri` **[string][214]** where to look for the data
-   `fields` **[Array][217]&lt;[FieldDefType][263]>** definition from which are generated getters

### reset

Reset the storage pointer, thus forcing it to lazily
download the data again.

Usable when the the off-chain data might have changed since
the last query and the most recent version of it is needed.

Returns **[Promise][222]&lt;void>** 

### \_genericGetter

Lazy data getter. The contents file gets downloaded only
once any data field is accessed for the first time. Also
the recursive `StoragePointer`s are created here only
after the contents of the data is known, because we need
to know the uri to be able to instantiate the appropariate
`StoragePointer`.

This behaviour might change in a way that we are able to
swap the StoragePointer implementation during runtime.

#### Parameters

-   `field` **[string][214]** 

Returns **([StoragePointer][235] \| [Object][215])** 

### \_detectSchema

Detects schema from an uri, i. e.
from `json://some-data`, detects `json`.

#### Parameters

-   `uri` **[string][214]** 

Returns **[string][214]?** 

### \_getOffChainDataClient

Returns appropriate implementation of `OffChainDataAdapterInterface`
based on schema. Uses `OffChainDataClient.getAdapter` factory method.

Returns **[OffChainDataAdapterInterface][249]** 

### \_initFromStorage

Sets the internal properties (\_data, \_storagePointers)
based on the data retrieved from the storage.

#### Parameters

-   `data` **[Object][215]** 

### \_downloadFromStorage

Gets the data document via `OffChainDataAdapterInterface`
and uses it to initialize the internal state.

Returns **[Promise][222]&lt;void>** 

### toPlainObject

Recursively transforms the off chain stored document to a sync plain
javascript object. By default, traverses the whole document tree.

You can limit which branches will get downloaded by providing a `resolvedFields`
argument which acccepts a list of paths in dot notation (`father.son.child`).
Every child will then get resolved recursively.

If you don't want some paths to get downloaded, just provide at least one sibling
field on that level which is not a `StoragePointer`. An empty list means no fields
will be resolved.

Data always gets downloaded if this method is called.

The resulting structure mimicks the original `StoragePointer` data structure:

    {
      'ref': 'schema://url',
      'contents': {
        'field': 'value',
        'storagePointer': {
          'ref': 'schema://originalUri',
          'contents': {
             'field': 'value'
          }
        },
        'unresolvedStoragePointer': 'schema://unresolved-url'
      }
    }

#### Parameters

-   `resolvedFields` **[Array][217]&lt;[string][214]>?** 
-   `list` **resolvedFields** of fields that limit the resulting dataset in dot notation (`father.child.son`).
     If an empty array is provided, no resolving is done. If the argument is missing, all fields are resolved.


-   Throws **[StoragePointerError][237]** when an adapter encounters an error while accessing the data

Returns **[Promise][222]&lt;{ref: [string][214], contents: [Object][215]}>** 

### createInstance

Returns a new instance of StoragePointer.

Normalizes the `fields` format before creating the actual
instance

#### Parameters

-   `uri` **[string][214]** where to look for data document. It has to include schema, i. e. `https://example.com/data`
-   `fields` **[Array][217]&lt;([FieldDefType][263] \| [string][214])>** list of top-level fields in the referred document

Returns **[StoragePointer][235]** 

## Utils

Collection of utility methods useful during
communication with Ethereum network.

### Parameters

-   `gasCoefficient` **[number][226]** 
-   `web3` **Web3** 

### isZeroAddress

Is address a zero address? Uses a string comparison.
Returns true also for strings that are not a valid address.

#### Parameters

-   `address` **[string][214]** 

Returns **[boolean][257]** 

### applyGasCoefficient

Multiplies the gas with a previously configured `gasCoefficient`

#### Parameters

-   `gas` **[number][226]** 

Returns **[number][226]** 

### getCurrentWeb3Provider

Proxy method for `web3.currentProvider`

Returns **[Object][215]** 

### getCurrentBlockNumber

Proxy method for `web3.eth.getBlockNumber`

Returns **[Promise][222]&lt;[number][226]>** 

### determineCurrentAddressNonce

Returns current number of transactions mined for given
Ethereum address

#### Parameters

-   `address` **[string][214]** 

Returns **[Promise][222]&lt;[number][226]>** number

### getTransactionReceipt

Proxy method for `web3.eth.getTransactionReceipt`

#### Parameters

-   `txHash` **[string][214]** 

Returns **[TxReceiptInterface][252]** 

### getTransaction

Proxy method for `web3.eth.getTransaction`

#### Parameters

-   `txHash` **[string][214]** 

Returns **[Promise][222]&lt;[TxInterface][264]>** 

### createInstance

Returns an initialized instance

#### Parameters

-   `gasCoefficient` **[number][226]** 
-   `web3` **Web3** instance created by `new Web3(provider)`

Returns **[Contracts][220]** 

## Wallet

Web3 based wallet implementation

### Parameters

-   `keystoreJsonV3` **[KeystoreV3Interface][231]** 

### setWeb3

Sets up an initialized Web3 instance for later use

#### Parameters

-   `web3` **Web3** 

### isDestroyed

It is not possible to do any operations on a destroyed
wallet. Wallet is destroyed by calling the `destroy()` method.

Returns **[boolean][257]** 

### getAddress

Returns the address passed in `keystoreJsonV3`
in a checksummed format, e.g. prefixed with 0x
and case-sensitive.

-   Throws **[WalletStateError][265]** When wallet was destroyed.
-   Throws **[WalletStateError][265]** When there's no keystore

Returns **[string][214]** 

### unlock

Unlocks/decrypts the JSON wallet keystore. <strong>From now on
there is a readable privateKey stored in memory!</strong>

#### Parameters

-   `password` **[string][214]** 


-   Throws **[WalletStateError][265]** When wallet was destroyed.
-   Throws **[WalletStateError][265]** When there is no web3 instance configured.
-   Throws **[WalletPasswordError][266]** When wallet cannot be decrypted.
-   Throws **[MalformedWalletError][267]** When wallet format is not recognized by web3.
-   Throws **[WalletError][268]** When anything else breaks down during decryption. But
    that should actually never happen unless the web3 implementation is changed.

### signAndSendTransaction

Takes transaction data, signs them with an unlocked private key and sends them to
the network. Resolves either immediately after receiving a `transactionHash` (with hash) or after
a `receipt` event (with raw receipt object). This depends on passed eventCallbacks.
When onReceipt callback is present, Promise is resolved after `receipt` event

#### Parameters

-   `transactionData` **[TransactionDataInterface][253]** 
-   `eventCallbacks` **[TransactionCallbacksInterface][254]?** 
-   `optional` **[TransactionCallbacksInterface][254]** callbacks called when events come back from the network


-   Throws **[WalletStateError][265]** When wallet was destroyed.
-   Throws **[WalletStateError][265]** When there is no web3 instance configured.
-   Throws **[WalletStateError][265]** When wallet is not unlocked.
-   Throws **[WalletSigningError][269]** When transaction.from does not match the wallet account.
-   Throws **[NoReceiptError][270]** When there are issues with getting a transaction receipt.
-   Throws **[OutOfGasError][271]** When it seems transaction ran out of gas
-   Throws **[TransactionRevertedError][272]** When it seems transaction was reverted in EVM
-   Throws **[InsufficientFundsError][273]** When it seems there is not enough ETH in this wallet
-   Throws **[InaccessibleEthereumNodeError][274]** When it seems the network is unreachable
-   Throws **[TransactionMiningError][275]** When there's another error during the signing and mining process

Returns **[Promise][222]&lt;([string][214] \| [TxReceiptInterface][252])>** transaction hash

### lock

Locks the wallet, i. e. deletes the private key from memory.
The original JSON keystore remains in the memory and can
be unlocked again if necessary.

This relies on the JS garbage collector, so please do not reference
the internal variables of this class elsewhere.

-   Throws **[WalletStateError][265]** When wallet was destroyed.

### destroy

Destroys the wallet. It first locks it, thus deleting
the private key from memory and then removes from
memory the JSON file.

This relies on the JS garbage collector, so please do not reference
the internal variables of this class elsewhere.

-   Throws **[WalletStateError][265]** When wallet was destroyed.

### createInstance

Creates an initialized instance

#### Parameters

-   `keystoreJsonV3` **[KeystoreV3Interface][231]** 

Returns **[Wallet][232]** 

[1]: #contracts

[2]: #parameters

[3]: #_getinstance

[4]: #parameters-1

[5]: #getindexinstance

[6]: #parameters-2

[7]: #gethotelinstance

[8]: #parameters-3

[9]: #decodelogs

[10]: #parameters-4

[11]: #createinstance

[12]: #parameters-5

[13]: #hoteldataindex

[14]: #properties

[15]: #hoteldescriptioninterface

[16]: #properties-1

[17]: #locationinterface

[18]: #properties-2

[19]: #additionalcontact

[20]: #properties-3

[21]: #contactinterface

[22]: #properties-4

[23]: #contactsinterface

[24]: #properties-5

[25]: #addressinterface

[26]: #properties-6

[27]: #datamodeloptionstype

[28]: #properties-7

[29]: #datamodel

[30]: #parameters-6

[31]: #getwindingtreeindex

[32]: #parameters-7

[33]: #gettransactionsstatus

[34]: #parameters-8

[35]: #createwallet

[36]: #parameters-9

[37]: #createinstance-1

[38]: #parameters-10

[39]: #onchainhotel

[40]: #parameters-11

[41]: #initialize

[42]: #dataindex

[43]: #setlocaldata

[44]: #parameters-12

[45]: #toplainobject

[46]: #parameters-13

[47]: #_editinfoonchain

[48]: #parameters-14

[49]: #createonchaindata

[50]: #parameters-15

[51]: #updateonchaindata

[52]: #parameters-16

[53]: #transferonchainownership

[54]: #parameters-17

[55]: #removeonchaindata

[56]: #parameters-18

[57]: #createinstance-2

[58]: #parameters-19

[59]: #wtindex

[60]: #parameters-20

[61]: #addhotel

[62]: #parameters-21

[63]: #updatehotel

[64]: #parameters-22

[65]: #removehotel

[66]: #parameters-23

[67]: #transferhotelownership

[68]: #parameters-24

[69]: #gethotel

[70]: #parameters-25

[71]: #getallhotels

[72]: #createinstance-3

[73]: #parameters-26

[74]: #wtlibserror

[75]: #parameters-27

[76]: #smartcontractinstantiationerror

[77]: #walleterror

[78]: #malformedwalleterror

[79]: #walletstateerror

[80]: #walletpassworderror

[81]: #walletsigningerror

[82]: #transactionminingerror

[83]: #outofgaserror

[84]: #insufficientfundserror

[85]: #transactionrevertederror

[86]: #noreceipterror

[87]: #inaccessibleethereumnodeerror

[88]: #inputdataerror

[89]: #offchaindataerror

[90]: #offchaindataconfigurationerror

[91]: #offchaindataruntimeerror

[92]: #storagepointererror

[93]: #remotelybackeddataseterror

[94]: #remotedataaccesserror

[95]: #remotedatareaderror

[96]: #wtlibsoptionstype

[97]: #properties-8

[98]: #wtlibs

[99]: #parameters-28

[100]: #getwtindex

[101]: #parameters-29

[102]: #gettransactionsstatus-1

[103]: #parameters-30

[104]: #createwallet-1

[105]: #parameters-31

[106]: #getoffchaindataclient

[107]: #parameters-32

[108]: #createinstance-4

[109]: #parameters-33

[110]: #errors

[111]: #hotelonchaindatainterface

[112]: #properties-9

[113]: #transactionoptionsinterface

[114]: #properties-10

[115]: #transactioncallbacksinterface

[116]: #properties-11

[117]: #preparedtransactionmetadatainterface

[118]: #properties-12

[119]: #hotelinterface

[120]: #properties-13

[121]: #wtindexinterface

[122]: #properties-14

[123]: #offchaindataadapterinterface

[124]: #properties-15

[125]: #datamodelinterface

[126]: #properties-16

[127]: #rawlogrecordinterface

[128]: #properties-17

[129]: #decodedlogrecordinterface

[130]: #properties-18

[131]: #transactiondatainterface

[132]: #properties-19

[133]: #txinterface

[134]: #properties-20

[135]: #txreceiptinterface

[136]: #properties-21

[137]: #adaptedtxresultinterface

[138]: #properties-22

[139]: #adaptedtxresultsinterface

[140]: #properties-23

[141]: #walletinterface

[142]: #properties-24

[143]: #keystorev3interface

[144]: #properties-25

[145]: #offchaindataclientoptionstype

[146]: #properties-26

[147]: #offchaindataclient

[148]: #setup

[149]: #parameters-34

[150]: #_reset

[151]: #getadapter

[152]: #parameters-35

[153]: #remotelybackeddataset

[154]: #bindproperties

[155]: #parameters-36

[156]: #isobsolete

[157]: #markobsolete

[158]: #isdeployed

[159]: #markdeployed

[160]: #_genericgetter

[161]: #parameters-37

[162]: #_genericsetter

[163]: #parameters-38

[164]: #updateremotedata

[165]: #parameters-39

[166]: #createinstance-5

[167]: #fielddeftype

[168]: #properties-27

[169]: #storagepointer

[170]: #parameters-40

[171]: #reset

[172]: #_genericgetter-1

[173]: #parameters-41

[174]: #_detectschema

[175]: #parameters-42

[176]: #_getoffchaindataclient

[177]: #_initfromstorage

[178]: #parameters-43

[179]: #_downloadfromstorage

[180]: #toplainobject-1

[181]: #parameters-44

[182]: #createinstance-6

[183]: #parameters-45

[184]: #utils

[185]: #parameters-46

[186]: #iszeroaddress

[187]: #parameters-47

[188]: #applygascoefficient

[189]: #parameters-48

[190]: #getcurrentweb3provider

[191]: #getcurrentblocknumber

[192]: #determinecurrentaddressnonce

[193]: #parameters-49

[194]: #gettransactionreceipt

[195]: #parameters-50

[196]: #gettransaction

[197]: #parameters-51

[198]: #createinstance-7

[199]: #parameters-52

[200]: #wallet

[201]: #parameters-53

[202]: #setweb3

[203]: #parameters-54

[204]: #isdestroyed

[205]: #getaddress

[206]: #unlock

[207]: #parameters-55

[208]: #signandsendtransaction

[209]: #parameters-56

[210]: #lock

[211]: #destroy

[212]: #createinstance-8

[213]: #parameters-57

[214]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[215]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[216]: #smartcontractinstantiationerror

[217]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[218]: #rawlogrecordinterface

[219]: #decodedlogrecordinterface

[220]: #contracts

[221]: https://github.com/windingtree/wt-js-libs/issues/125

[222]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise

[223]: #locationinterface

[224]: #contactsinterface

[225]: #addressinterface

[226]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

[227]: #additionalcontact

[228]: #contactinterface

[229]: #datamodeloptionstype

[230]: #adaptedtxresultsinterface

[231]: #keystorev3interface

[232]: #wallet

[233]: #datamodel

[234]: #utils

[235]: #storagepointer

[236]: #hotelonchaindatainterface

[237]: #storagepointererror

[238]: #transactionoptionsinterface

[239]: #preparedtransactionmetadatainterface

[240]: #onchainhotel

[241]: #inputdataerror

[242]: #wtlibserror

[243]: #hotelinterface

[244]: #wtindex

[245]: #offchaindataclientoptionstype

[246]: #wtlibsoptionstype

[247]: #wtindexinterface

[248]: #walletinterface

[249]: #offchaindataadapterinterface

[250]: #wtlibs

[251]: https://web3js.readthedocs.io/en/1.0/web3-eth-contract.html#contract-estimategas

[252]: #txreceiptinterface

[253]: #transactiondatainterface

[254]: #transactioncallbacksinterface

[255]: https://web3js.readthedocs.io/en/1.0/web3-eth-accounts.html#signtransaction

[256]: http://web3js.readthedocs.io/en/1.0/web3-eth.html#gettransaction

[257]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Boolean

[258]: https://medium.com/@julien.m./what-is-an-ethereum-keystore-file-86c8c5917b97

[259]: https://github.com/ethereum/wiki/wiki/Web3-Secret-Storage-Definition

[260]: #offchaindataconfigurationerror

[261]: #offchaindataruntimeerror

[262]: #remotedataaccesserror

[263]: #fielddeftype

[264]: #txinterface

[265]: #walletstateerror

[266]: #walletpassworderror

[267]: #malformedwalleterror

[268]: #walleterror

[269]: #walletsigningerror

[270]: #noreceipterror

[271]: #outofgaserror

[272]: #transactionrevertederror

[273]: #insufficientfundserror

[274]: #inaccessibleethereumnodeerror

[275]: #transactionminingerror
